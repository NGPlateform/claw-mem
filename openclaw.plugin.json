{
  "id": "claw-mem",
  "name": "Claw-Mem Semantic Memory",
  "version": "0.1.0",
  "description": "Persistent cross-session semantic memory for OpenClaw AI agents. Captures observations from tool usage, compresses into summaries, injects relevant context on session start.",
  "main": "index.ts",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": { "type": "boolean", "default": true },
      "dataDir": { "type": "string", "default": "" },
      "tokenBudget": { "type": "number", "default": 8000 },
      "maxObservations": { "type": "number", "default": 50 },
      "maxSummaries": { "type": "number", "default": 10 }
    }
  }
}
